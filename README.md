# Funny Bunny

Динамическая игровая сцена (клон Angry Birds), реализованная на PIXI.js и Matter.js. 
Проект собран в один автономный HTML-файл.

1.  **Готовый файл:** Находится в папке `dist/index.html`. Его можно запустить локально в любом браузере.
2.  **Исходники:** Расположены в папке `src/`.


## 🛠 Как собрать и запустить

### Запуск в режиме разработки:
1. Установите зависимости: `npm install`
2. Запустите сервер: `npm run dev`

### Сборка финального index.html:
1. Выполните команду: `npm run build`
2. Сборщик Vite с плагином `vite-plugin-singlefile` упакует JS, CSS и логику в файл `dist/index.html`.

## ⚙️ Конфигурация (Config)
Все игровые настройки вынесены в файл `src/config.ts`. Там можно изменить:
* Параметры натяжения рогатки.
* Физические свойства объектов.
* Тайминги сессии (20 секунд).

## 🤖 Использование AI-инструментов
При разработке использовался **Gemini AI**:
* Расчет траектории полета снаряда (парабола).
* Оптимизация конфига Vite для Single File сборки.
* Исправление конфликтов типов между PIXI и Matter.js.

## 💡 Допущения
* **Ошибки в IDE:** В собранном файле `dist/index.html` редакторы кода (VS Code) могут подсвечивать ошибки синтаксиса. Это ложноположительный результат, вызванный экстремальной минификацией и огромной длиной строк в одном файле. В браузере код выполняется корректно.
